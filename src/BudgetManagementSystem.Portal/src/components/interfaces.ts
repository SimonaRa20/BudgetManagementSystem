export interface User {
  id: number;
  userName: string;
  role: string;
  token: string;
  refreshToken: string;
}
  export interface Family {
    id: number;
    title: string;
    membersCount: number;
  }
