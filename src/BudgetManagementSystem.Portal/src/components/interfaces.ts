export interface User {
    id: number;
    userName: string;
    userRole: string;
    userToken: string;
    refreshToken: string;
  }
  export interface Family {
    id: number;
    title: string;
    membersCount: number;
  }
